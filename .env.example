# Local / default (usually points at Supabase develop = staging)
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_KEY=

# Optional: only needed for admin scripts (seed, etc.)
# SUPABASE_SERVICE_ROLE_KEY=
